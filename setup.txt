brew install zsh
brew install zsh-autosuggestions
brew install jandedobbeleer/oh-my-posh/oh-my-posh
brew install tmux
brew install tpm
brew install neovim
brew install fzf
brew install eza
brew install bat
brew install cmatrix
brew install lolcat
brew install tldr
brew install stow
brew install docker
brew install lazygit
brew install lazydocker
brew install bruno
brew install yazi
brew install ripgrep
brew install kubernetes-cli
brew install fastfetch
brew install btop

brew install rust
brew install rust-enalyzer
brew install --cask google-cloud-sdk
brew install --cask slack
brew install phpstorm
brew install npm
brew install nvm

brew install postgresql@17
brew services start postgresql@17


# zsh setup
command -v zsh | sudo tee -a /etc/shells
sudo chsh -s $(which zsh)
sudo usermod --shell $(which zsh) $(whoami)

# nvim setup
cd ~/.config
git clone git@github.com:spookyfish/kickstart.nvim.git "${XDG_CONFIG_HOME:-$HOME/.config}"/nvim

# tmux setup
check tpm install path as it may have changed
brew info tpm
Ctrl+s I # to make sure plugins are installed


# symlink dotfiles (assuming you have ~/dotfiles cloned)
ln -s ~/dotfiles/.zshrc ~
ln -s ~/dotfiles/.tmux.conf ~
## Updated version uses stow
cd ~/dotfiles
stow zsh tmux ohmyposh

# fonts
brew search nerd-font
brew install --cask font-fira-code-nerd-font

# github
ssh-keygen -t ed25519 -C "icha023@gmail.com"
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
cat ~/.ssh/id_ed25519.pub
# github profile -> Settings -> SSH and GPG keys -> New SSH key
